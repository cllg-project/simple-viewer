[pytest]
# Only collect this project's tests. The cloned corpus (./corpus) ships its own
# test suite which must not be picked up by `make test` / CI.
testpaths = tests
norecursedirs = corpus .venv .git tests/fixtures
